package com.golf.app.repo;

import com.golf.app.model.RoundScore;
import org.springframework.data.repository.CrudRepository;

public interface RoundScoreRepository extends CrudRepository<RoundScore, Long> {
}
